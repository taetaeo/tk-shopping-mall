import React, { FC } from "react";
import styled from "styled-components";
type Props = {};

const BottomInfo: FC<Props> = (props: Props): JSX.Element => {
  const {} = props;
  return <></>;
};
export default BottomInfo;
